model:
  num_islands: 2
  input_dim: 64
  hidden_dim: 32
  ocean_dim: 16
  top_k: 1
  max_islands: 4
  min_islands: 1
  coherence_variance_threshold: 0.8

training:
  lr: 0.001
  epochs: 2
  batch_size: 32
  log_every: 5
  save_every: 5
  checkpoint_dir: checkpoints_test
  device: cpu

data:
  num_samples: 500
  input_dim: 64
  num_classes: 5
  signal_strength: 0.5