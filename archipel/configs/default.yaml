model:
  num_islands: 4
  input_dim: 128
  hidden_dim: 64
  ocean_dim: 32
  top_k: 2
  max_islands: 8
  min_islands: 2
  coherence_variance_threshold: 0.3

training:
  lr: 0.001
  epochs: 50
  batch_size: 16
  log_every: 10
  save_every: 10
  checkpoint_dir: checkpoints
  device: cpu

data:
  num_samples: 2000
  input_dim: 128
  num_classes: 10
  signal_strength: 0.7
